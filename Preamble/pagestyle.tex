% --- Page Geometry ---
\usepackage[margin=1in]{geometry}

% --- Paragraph Setup ---
\onehalfspacing
\setlength{\parindent}{0.5in}
\setlength{\parskip}{0pt}

% --- Graphics Path ---
\graphicspath{{assets/}}

% --- Header and Footer ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{%
	\ifnum\value{page}>0
	\includegraphics[height=1.2cm]{header.png}\hspace*{-1.5cm}
	\fi
}
% \fancyhead[L]{\leftmark} % Removed to avoid title repetition on top left
\fancyfoot[C]{%
	\parbox{\linewidth}{%
		\centering
		\thepage\\[5pt]
		\ifnum\value{page}>3
		\makebox[0pt][c]{\includegraphics[width=\paperwidth]{footer.png}}
		\fi
	}%
}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\setlength{\headheight}{35pt}
\setlength{\headsep}{15pt} % Reduces the empty space before the content

% Apply fancy style to chapter pages as well
\fancypagestyle{plain}{%
	\fancyhead[R]{%
		\ifnum\value{page}>0
		\includegraphics[height=1.2cm]{header.png}\hspace*{-1.5cm}
		\fi
	}
	\fancyfoot[C]{%
		\parbox{\linewidth}{%
			\centering
			\thepage\\[5pt]
			\ifnum\value{page}>3
			\makebox[0pt][c]{\includegraphics[width=\paperwidth]{footer.png}}
			\fi
		}%
	}
	\renewcommand{\footrulewidth}{0pt} 
}